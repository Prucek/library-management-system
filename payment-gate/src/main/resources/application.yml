management:
  endpoints:
    web:
      exposure:
        include: "health,metrics,prometheus"
  endpoint:
    health:
      show-details: always
      show-components: always
server:
  port: 8081
  error:
    include-message: always
